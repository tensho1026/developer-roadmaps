# iOS Xcode app

SwiftUI / UIKit / Combine / Core Data。シミュレータで:

```bash
xcodebuild -project RoadmapsApp.xcodeproj -scheme RoadmapsApp -destination 'platform=iOS Simulator,name=iPhone 16' build
```

または `RoadmapsApp.xcodeproj` を Xcode で開く。
