[project]
name = "back-python"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy",
  "psycopg[binary]",
  "pydantic-settings",
  "python-jose[cryptography]",
  "redis",
  "httpx",
  "pytest",
]
