# Prisma + OpenAPI

Node.js ロードマップの ORM（Prisma）と、Backend の Open API Specs。

```bash
cp .env.example .env
# リポジトリルートで docker compose up -d postgres
npx prisma db push
npm run dev
```

- CRUD: http://localhost:3020/users
- Swagger UI: http://localhost:3020/docs
